{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://blooperz.github.com/language_courant.git",
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-flexview": "^4.0.1",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
